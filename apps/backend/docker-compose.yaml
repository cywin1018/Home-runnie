name: jikgwan-backend

services:
  backend:
    build:
      context: ../../
      dockerfile: apps/backend/Dockerfile
    ports:
      - 3030:3030
    environment:
      - NODE_ENV=production
    env_file:
      - ./secret/.env
